server:
  port: 8760

spring:
  application:
    name: API-GATEWAY


zuul:
  routes:
    PROBLEM-SERVICE:
      path: /problem/**
      serviceId: PROBLEM-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost